[build-system]
requires = ["setuptools>=46.1.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["src"]
filterwarnings = [
    "ignore:html5lib's sanitizer is deprecated:DeprecationWarning",
    "ignore:pkg_resources is deprecated:UserWarning",
]
